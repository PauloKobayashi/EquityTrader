# Realtime cockpit webui (webui/). Plotly.js is loaded from CDN, so there is no
# client-side server dependency here.
flask>=3.0
numpy>=1.24

# Optional — only needed to pick a robot from a zion GE run (the run-picker is
# hidden when these / zion_ge are not importable):
# pymongo>=4.0
